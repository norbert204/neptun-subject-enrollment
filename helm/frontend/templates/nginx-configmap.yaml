apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-nginx-conf
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  default.conf.template: |
    server {
      listen 80;
      server_name  localhost;

      root /usr/share/nginx/html;
      index index.html;

      # Serve static files
      location / {
        try_files $uri $uri/ /index.html;
      }

      # Special-case user endpoints: Admin controller exposes user routes under /Admin/user
      location /api/user/ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://{{ .Values.serviceLocation.gatewayHost }}:80/Admin/user/;
      }
      # exact match for /api/user without trailing slash
      location = /api/user {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://{{ .Values.serviceLocation.gatewayHost }}:80/Admin/user;
      }

      # Proxy other API requests to gateway inside cluster (strip /api prefix)
      location /api/ {
        rewrite ^/api/(.*)$ /$1 break;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://{{ .Values.serviceLocation.gatewayHost }}:80;
      }

      # Proxy other service calls if needed (e.g., grpc-web)
      location /grpc/ {
        proxy_pass http://{{ .Values.serviceLocation.gatewayHost }}:80;
        proxy_set_header Host $host;
      }
    }

  nginx.conf: |
    user  nginx;
    worker_processes auto;
    pid /var/run/nginx/nginx.pid;

    events {
      worker_connections 1024;
    }

    http {
      include       /etc/nginx/mime.types;
      default_type  application/octet-stream;
      sendfile        on;
      keepalive_timeout 65;
      include /etc/nginx/conf.d/*.conf;
    }
