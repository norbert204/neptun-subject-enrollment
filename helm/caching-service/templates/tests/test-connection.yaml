apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caching-service.fullname" . }}-test-connection"
  labels:
    {{- include "caching-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caching-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
