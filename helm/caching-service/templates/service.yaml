apiVersion: v1
kind: Service
metadata:
  name: {{ include "caching-service.fullname" . }}
  labels:
    {{- include "caching-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caching-service.selectorLabels" . | nindent 4 }}
