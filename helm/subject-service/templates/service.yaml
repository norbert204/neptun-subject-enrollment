apiVersion: v1
kind: Service
metadata:
  name: {{ include "subject-service.fullname" . }}
  labels:
    {{- include "subject-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "subject-service.selectorLabels" . | nindent 4 }}
